# Dayglow

More than just dark mode for xkcd comics

## Running

There's a cloned version of the site with ``<script src="../modes.js"></script>`` added to the header. To run locally you can `nix run nixpkgs#simple-http-server` and go to `http://localhost:8000/site/index.html`